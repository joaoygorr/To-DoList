info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/todo"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  body:
    type: json
    data: |-
      {
        "title": "teste",
        "description": "teste",
        "status": "PENDENTE", 
        "deadLine": "26/04/2002"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
